---
import Footer from "../components/Footer.astro";
import type { TitleProps } from "../utils/types";
import Layout from "./Layout.astro";

interface Props extends TitleProps {}

const { title, imageSrc, imageAlt } = Astro.props;
---

<Layout title={title} imageSrc={imageSrc} imageAlt={imageAlt}>
  <div>
    <main>
      <h1>{title}</h1>
      <slot />
    </main>
    <hr />
    <Footer title={title} />
  </div>
</Layout>

<style>
  main {
    padding: 10px;
    min-height: 100dvh;
  }
</style>
